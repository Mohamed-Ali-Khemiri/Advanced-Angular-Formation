import { DirHoverDirective } from "./dir-hover.directive";

describe('DirHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new DirHoverDirective();
    expect(directive).toBeTruthy();
  });
});
